# Shared

Place code here only when it is genuinely used by both the frontend and the
backend, such as shared constants, validation helpers, or common types.

Keep this folder small so the app stays easy to split into focused commits.